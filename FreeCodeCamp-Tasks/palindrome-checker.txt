function palindrome(str) {
  var punct = /[\():/,_." "\-]/ig;
  let newStr = str.replace(punct,"");
  let arr = newStr.toUpperCase().split("");
  for(let i = 0 ; i < arr.length ; i++){
    if(arr[i] !== arr[arr.length-(i+1)] ){
      return false ;
    }
  }
  return true ;
}
  

palindrome("eye");